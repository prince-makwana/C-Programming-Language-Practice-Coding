using System;

/*
    Events: With events, object can notify other classes whenever something happens. 
    The class that raises the event is called publisher and the class that received 
    the event notifications are called subscribers.
 */
namespace EventsDemoApp
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
        }
    }
}
